begin
    var x : integer = 10;
    
    if (x > 5) then
    begin
        write("x is greater than 5");
    end
    else
    begin
        write("x is not greater than 5");
    end
    
    var y : integer = 3;
    
    if (y > 5) then
    begin
        write("y is greater than 5");
    end
    else
    begin
        write("y is not greater than 5");
    end
end
